import("math");
import("stdio");

func factorial(n) {
	if (n <= 1) return 1;
	var f = 1;
	for (var i = 1; i <= n; i = i + 1)
		f = f * i;
	return f;
}

var x = read("Insert an integer number: ");
print "Factorial of " + x + " is " + factorial(x);
